# Проверка работающего приложения и готовности пилота

## Подготовка курса и продолжение тренировок — 20.09.2026

Выпуск с миграцией `0023_course_review_study.sql`. В работающем локальном приложении (FastAPI, PostgreSQL, Redis, настоящий поиск E5 и Letovo DeepSeek; синтетический Canvas/LTI) пройдены новые пользовательские пути. [Экран готовности курса](screenshots/preparation-readiness.png).

- Преподаватель открыл неопубликованный `bloom-audit-paragraphs.txt` из экрана готовности и спросил о продуктах световой фазы. Получен ответ «АТФ и НАДФН» с источником этого черновика. Материал остался черновиком; после обновления сводки появился результат проверки его текущей версии. Снимок: [проверка черновика](screenshots/preparation-draft-preview.png).
- Для старого тестового фрагмента со склеенным заголовком преподаватель добавил «Запомнить» и фактическое знание, сохранил обоснование. После повторного открытия анализа правка сохранилась; автоматические метки и счётчики остались отдельными. Снимок: [экспертная правка](screenshots/preparation-bloom-review.png).
- Ученик начал настоящую тренировку по фотосинтезу, набрал ответ и дождался автосохранения. После перезагрузки «Продолжить тренировку» восстановило тот же вопрос и полный черновик. Модель приняла ответы, создала второй и третий вопросы; завершение показало результаты всех трёх. Снимок: [результаты на ширине 390 px](screenshots/preparation-study-mobile.png). Проверка не является независимой оценкой педагогического качества генерации; вопросы могут пересекаться по проверяемому содержанию.
- В ручном обходе найден и исправлен зависающий текст «Сохраняем ответ…» после отправки попытки. Также переведён технический статус завершённого импорта. На мобильной ширине нет горизонтального переполнения; форма правки сохраняет читаемость и доступность элементов.

Серверная проверка: **204 Python-теста**, включая **37 интеграционных сценариев PostgreSQL/Redis**, проходят. Новые сценарии проверяют запрет preview для ученика через оба chat API, отсутствие публикации и записи в журнал, сохранение и конфликт версий Блума, изоляцию курса, изменение текста и индекса, три вопроса, черновик, владельца/роль, истечение хранения, отзыв источника и закрытое решение. Два предупреждения тестового клиента остаются. Production build и Ruff изменённых production-модулей проходят. **34 сценария Playwright desktop/mobile** проходят на итоговой сборке с подменённым API: проверка черновика, сводка, сохранение правки после перезагрузки, продолжение трёх вопросов, восстановление результата и отсутствие зависшего статуса сохранения. Включены размеры 320/390/768/1440 px.

Учебное состояние теперь персональное и хранится в PostgreSQL 7 дней после изменения; преподаватель не получает API-доступ к нему. Проверка чата преподавателем не доказывает истинность ответа. Школьный Canvas, реальные индивидуальные права, HTTPS/OIDC и новая полная Compose-сборка в этой итерации не проверялись. Подробности обновления: [техническое руководство](TECHNICAL_GUIDE.md), [руководство пользователя](USER_GUIDE.md).

Дата: 19 сентября 2026 года. Ветка: `JD/canvas-lti-handoff`. Проверка включает ручное прохождение приложения в браузере, а не только чтение кода. Документация: [техническая](TECHNICAL_GUIDE.md), [для пользователей](USER_GUIDE.md), [для администратора Canvas](CANVAS_ADMIN_HANDOFF.md).

## Контекстная разметка Блума — 19.09.2026

В анализе курса прежние ключевые слова заменены воспроизводимой рубрикой `rbt-context-1.0`: явное действие ученика, объект и условия задания; когнитивный процесс и тип знания учитываются отдельно. Для каждого предложения метки показаны цитата, правило, обоснование и переход к исходному материалу. Неоднозначные задания требуют проверки. Научная основа, ограничения и протокол будущей экспертной валидации описаны в [методологии](BLOOM_METHODOLOGY.md).

**Проверки итогового изменения:** 199 Python-тестов (включая интеграционные PostgreSQL/Redis), 30 сценариев Playwright desktop/mobile и production-сборка проходят. Ruff проходит для изменённых production-модулей, нового набора тестов и скрипта оценки. Сохраняются два предупреждения об устаревающих API тестового клиента. Playwright использует подменённый API; сценарии проверяют доказательства классификации, научные ссылки, открытие контекста и ширины 320/390/768/1440 px.

В работающем локальном приложении с настоящими FastAPI, PostgreSQL и Redis проверены:

- Два описательных PDF/DOCX о фотосинтезе не получают искусственно назначенный уровень «Создать». Явная инструкция объяснить и сравнить на странице курса получает «Понять».
- Через форму загружен контрольный TXT: распознаны задания всех шести категорий; «Создайте модель клетки по готовому образцу» получает «Применить», а «Оцените этот материал» остаётся без уровня, с объяснением недостатка контекста.
- Найдена и исправлена потеря границ между заголовком и первым заданием при загрузке: новый файл сохраняет короткие инструкции и абзацы. Старые материалы для восстановления этих границ нужно загрузить или импортировать повторно. Разбиение PDF сохранено отдельно из-за переносов строк в текстовом слое.
- Обоснования и длинные заголовки проверены на настольном экране и при ширине 390 px; горизонтального переполнения нет. Снимки: [обоснование](screenshots/bloom-rubric-evidence.png), [мобильный экран](screenshots/bloom-rubric-mobile.png).

Новая рубрика проходит 40 авторских регрессионных примеров, включая 9 описательных фрагментов без задания и 8 неоднозначных заданий. Это примеры, использованные при разработке, **не независимая научная оценка точности**. [Машинный отчёт](bloom-regression-report.json) сохраняет сравнение, версии окружения и ограничение старого классификатора при обнаружении морфологического словаря. Независимая разметка школьных курсов несколькими экспертами ещё не проведена. LLM для этой классификации не вызывается. Старый классификатор Studio не изменён. Проверка школьного Canvas и новой Docker-сборки в этой итерации не выполнялась.

Далее сохранены результаты предыдущих итераций; приведённые в них меньшие числа тестов относятся к соответствующему выпуску.

## Повторный аудит логики продукта и UX — 19.09.2026

Описаны [роли, пользовательские пути, состояния и приоритеты](PRODUCT_LOGIC.md). Исправлены устаревшие настройки журнала в открытом чате, обновление библиотеки, ввод комментария без получателя, подсказка черновика, обещание обезличивания и отображение сетевой ошибки упражнения.

**Итоговая production-сборка и 28 сценариев Playwright desktop/mobile проходят.** API в этих автоматических сценариях подменён. Новый регрессионный сценарий проверяет смену настроек журнала без потери черновика, сброс согласия после отключения, повтор загрузки после сбоя и обновление материалов. Дополнительно проверены разрыв соединения при ответе на упражнение, сохранность введённого текста и успешный повтор; ошибка находится внутри задания. Проверки размеров 320/390/768/1440 px входят в набор. Backend в этой итерации не менялся; 154 Python-теста ниже — результат предыдущего выпуска, повторно в этой итерации не запускались.

Ручной обход выполнен в локальном приложении с настоящими PostgreSQL, Redis, поиском E5 и Letovo DeepSeek; Canvas и вход — синтетический тестовый курс. Через форму загружен DOCX, проверены текст и извлечённая схема, материал опубликован и открыт учеником. Реальная модель дала структурированный ответ со схемой, исходной иллюстрацией и ссылками на этот DOCX, затем создала упражнение и приняла верный ответ. Оценка материала появилась в сводке преподавателя. После изменения настройки журнала на итоговой сборке ученик увидел новый флажок при возврате в чат; набранный вопрос сохранился.

Повторный импорт через интерфейс завершился отчётом: страница и PDF импортированы, неопубликованная страница пропущена. Ученик перестал видеть снятые с публикации копии; после повторной публикации «Обновить материалы» вернуло их в библиотеку. Дополнительный DOCX остался доступен. Название страницы теперь полное, без технического slug. Проверены внешний вид библиотеки и новая форма отзыва на реальной ширине 390 px: горизонтального переполнения нет. На настольном экране материал и чат располагаются в отдельных прокручиваемых колонках.

Снимки: [настоящий ответ по DOCX](screenshots/ux-review-docx-answer.png), [принятый ответ упражнения](screenshots/ux-review-docx-practice.png), [форма оценки на мобильном экране](screenshots/ux-review-feedback-mobile.png).

На момент этой итерации эвристика Блума ошибочно относила описание образования веществ к «Создать». В последующем изменении выше она заменена контекстной рубрикой. Школьный Canvas, HTTPS/OIDC, реальные права курса и нагрузка по-прежнему требуют отдельной приёмки.

## Выпуск рабочего пространства — 19.09.2026

Реализованы и проверены: чат рядом с материалом и выделением фрагмента; страничные цитаты PDF; упражнение с попытками, подсказкой и разрешением решения; добровольный журнал качества без ID ученика; импорт модулей и файлов с прогрессом; потоковый ответ с остановкой/повтором; локальные история и заметки.

Ручные проверки в настоящем приложении (Letovo DeepSeek, E5, PostgreSQL/Redis):

- Импорт синтетического курса завершился: страница и PDF в черновиках, неопубликованная страница пропущена, модуль показан в библиотеке. Проверена публикация обеих копий.
- Ученик получил ответ по странице курса и PDF: объяснение по шагам, схему, исходную иллюстрацию и цитату с номером PDF-страницы.
- После согласия ученика синтетический вопрос с именем «Иван Петров» и адресом test@example.org появился у преподавателя как `[имя]` и `[контакт]`. Отметка «Полезно» появилась в журнале. Вопрос без согласия не записывается (проверено также интеграционным тестом).
- Упражнение: неверная попытка получила подсказку; правильная вторая попытка принята; решение открылось только после второй попытки согласно настройке курса. Оценки Canvas не создавались.
- PDF открыт по источнику, фрагмент выбран кнопкой, вопрос по нему отправлен. Проверены остановка настоящего запроса и повтор: незавершённый текст удалён, вопрос восстановлен, повторный ответ получен с источниками.
- Окно материала проверено визуально: две колонки на широком экране; на 390 px — последовательное расположение, горизонтальное переполнение отсутствует. Исправлена прокрутка, при которой поле чата уводило материал за экран. Нативный предпросмотр исходного PDF во встроенном браузере Codex оказался недоступен: добавлены проверка поддержки, понятное сообщение и путь через скачивание оригинала. Полный нативный PDF-просмотр на целевом школьном браузере требует приёмки; точные координаты выделения внутри PDF не заявляются.

Снимки действующего приложения: [упражнение](screenshots/workspace-practice-live.png), [обезличенный журнал](screenshots/workspace-quality-live.png), [мобильный просмотр материала](screenshots/workspace-reader-mobile-live.png). Все имена/контакты в проверке синтетические. На снимках журнала видно также ограничение NER: фамилия Кальвина может быть скрыта как имя.

Автоматическая проверка нового выпуска: **154 Python-теста**, включая **31 интеграционный сценарий PostgreSQL/Redis**, без пропусков. Проверены принадлежность выделенного фрагмента, запрет подставной цитаты, opt-in и отказ записи при сбое NER, доступ преподавателя только к своему курсу, capability оценки, удаление старых записей, владение упражнением и запрет раннего решения, отзыв источника, потоковый ответ/ошибка, модули/файлы, повторный импорт без дубликатов и отзыв при ограничении модуля. Два предупреждения об устаревающих API тестового клиента остаются. Production-сборка и Ruff проходят. **26 сценариев Playwright desktop/mobile** проходят с подменённым API; включены разбор фрагмента, две попытки и открытие решения, согласие на журнал, сохранение/удаление заметок, остановка и повтор, настройки и просмотр журнала на 320/390/768/1440 px.

Ниже описана также проверка предыдущего выпуска; прежние числа автоматических тестов относятся к нему.

## Среда и границы проверки

Запущены production-сборка Next.js на loopback-порту 3100, настоящий FastAPI на 8000, PostgreSQL 16 с pgvector и Redis 7 в отдельных тестовых контейнерах. Использована реальная локальная `multilingual-e5-small` на CPU и реальная Letovo `deepseek-v4-flash`. Учебный текст о фотосинтезе создан специально для проверки; реальных данных учеников нет.

Две браузерные вкладки получили роли преподавателя и ученика через подписанный тестовый LTI-запуск. `/lti/login` и `/lti/launch` выполнялись настоящим приложением; ключ платформы и страницы отсутствующего школьного Canvas предоставляла локальная тестовая оболочка. Она находится только в игнорируемом каталоге и не входит в поставку. Это **не проверка реального браузерного OIDC-перехода через школьный Canvas**, его cookie, Developer Key или сетевых прав REST API.

Автоматические UI-тесты используют подменённый API. Их результаты ниже отделены от ручных действий с работающим сервером.

## Что проверено руками через интерфейс

| Сценарий | Наблюдавшийся результат |
|---|---|
| Вход преподавателя и ученика | Верное название курса и роль; у ученика только чат и материалы |
| Пустой курс | Понятные пустые состояния, нули по шести уровням Блума и четырём метрикам |
| Загрузка MD, DOCX и PDF | Файлы появляются черновиками; извлечённый текст читается в диалоге. PDF проверен с текстовым слоем |
| Иллюстрации PDF/DOCX | Через браузер загружены два документа со схемой фотосинтеза: извлечено по одной картинке. В просмотре показаны подпись DOCX и номер страницы PDF |
| Чат с изображением | Реальный DeepSeek ответил о связи фаз фотосинтеза и приложил схему из опубликованного DOCX; доступна кнопка перехода к материалу |
| Публикация | Опубликованная литература доступна ученику, черновики не отображаются |
| Импорт страниц | Из двух тестовых страниц импортирована одна опубликованная, неопубликованная пропущена; копия остаётся черновиком |
| Анализ Блума | Для учебного фрагмента показаны уровни и пример; есть предупреждение об эвристике и включении черновиков |
| Настоящий чат | На вопрос о связи световой фазы и цикла Кальвина получен ответ с цитатой из литературы |
| Уточняющий вопрос | На «А где происходит каждая из этих фаз?» получен ответ о тилакоидах и строме с источником |
| Вопрос вне темы | На вопрос об автомобильном двигателе показан отказ из-за недостатка информации |
| Отзыв ученика | Оценка «Сложно» и тестовый комментарий сохранены; преподаватель видит категорию и «менее 5», без имени и текста комментария |
| Скрытие материала | Старый пункт списка уже не открывается; после обновления библиотека ученика пуста |
| Метрики | В контрольной точке: 4 вопроса, 2 ответа с источниками, 1 отказ и 1 ошибка; счётчики соответствовали действиям |
| Мобильный интерфейс | Библиотека и отзыв проверены при 390 px, пустая библиотека — при 320 px; горизонтального переполнения не обнаружено |
| Desktop | Просмотрены библиотека, чат с раскрытой цитатой, анализ и сводка; пересечений текста и кнопок не обнаружено |

Часть живых запросов к модели завершилась ошибкой; были также успешные ответы и повторные отправки. Поле сохраняло вопрос для повтора, а в финальной сборке ошибка была видна рядом с ним. После обновления формата E5 и загрузки нового DOCX вновь получен корректный ответ с цитатой: цикл Кальвина происходит в строме хлоропласта. Отдельный короткий запрос к DeepSeek также прошёл, а контрольный запрос к Gemma завершился тайм-аутом. **Проверенная конфигурация чата — DeepSeek; стабильная доступность Letovo и надёжность Gemma не подтверждены.** Один успешный диалог не является оценкой качества RAG на полном курсе.

Автоматическое скачивание весов затянулось; для проверки использована полная локальная копия из официального репозитория Hugging Face. `/health` подтвердил `semantic=true`. Первый запуск и загрузку модели необходимо подготовить на школьном сервере. Физический iPhone и Safari не проверялись.

## Исправления по результатам проверки

- Ошибка чата отображается у поля ввода, а не за пределами видимой области в шапке. Неудавшийся вопрос восстанавливается без дублирования истории.
- Длинный ответ больше не ломает следующий запрос: история ограничивается серверным лимитом на сообщение.
- Сообщения о недоступном материале, лимите запросов и недоступности сервиса переведены на русский.
- Уточнены ограничения файлов/PDF, добавлена предварительная проверка размера; нулевые показатели видны.
- Исправлено поведение длинных названий и кнопок материалов на узких экранах.
- Пустой ответ модели с формально допустимыми цитатами больше не считается успешным. Ошибки поиска учитываются в метриках.
- Для E5 добавлены рекомендованные префиксы документов и запросов. Версия векторов изменена; при обновлении нужна повторная индексация по техническому руководству.
- Главная страница отдельной LTI-сборки ведёт в портал. Контекст Docker исключает локальные виртуальные окружения, сборки Next.js и браузерные отчёты.

## Дополнительные воспроизводимые проверки

Дополнительно через живой интерфейс импортирована синтетическая страница Canvas с прикреплённой картинкой: результат — одна страница и одна иллюстрация. После публикации настоящий Letovo DeepSeek выдал объяснение по шагам, схему связей и исходную картинку страницы. У каждого блока доступны источники. Сервер, БД, поиск и модель настоящие; API школьного Canvas и скачивание его тестового изображения представлены локальной проверочной средой, поэтому проверка не подтверждает доступ к файловому хранилищу школы.

Автотесты дополнительно проверяют снятие доступа при скрытии/замене картинки и изменении только HTML/alt, повторный импорт без дубликатов, блокировку внешних файлов и перенаправлений вне разрешённых origin, отсутствие Canvas-токена в запросе к CDN, источники схем и отказ от неверных структурированных блоков.

В живом API отдельно проверен `response_style=check`: Letovo вернул «Какой продукт световой фазы используется в цикле Кальвина, и для чего он там нужен?» с допустимой цитатой; разделы и картинки отсутствовали, схема — null. Для этого режима уточнена инструкция модели: ссылаться на материал, по которому можно проверить ответ ученика, не раскрывая сам ответ. Отправка выбранного кнопкой формата проверена браузерным тестом.

| Проверка | Результат и охват |
|---|---|
| Python | 137 тестов пройдено, в том числе 26 с PostgreSQL/Redis; пропущенных нет. Два предупреждения об устаревающих API тестового клиента |
| Backend-интеграция | Подпись/replay LTI, роли, изоляция курсов, публикация, импорт, удаление связанных данных, DOCX, ограничения загрузки, ошибки модели, недопустимые цитаты, отзыв доступа во время ответа, лимиты |
| Frontend production build | Успешно; TypeScript и ESLint без ошибок |
| Playwright | 20 сценариев desktop/mobile; API подменён. Все четыре раздела преподавателя при 320/390/768/1440 px, длинные названия, модальные окна, повтор запроса, сессия, картинки, скачивание оригинала, структурированные ответы, схемы с источниками, кнопки продолжения и Studio |
| Ruff | Изменённые Python-модули и тесты без замечаний |
| Docker | Полная первоначальная сборка backend/frontend выполнена. Повторная сборка финальных изменений блокировалась Docker Hub: TLS timeout и 401 при получении базовых образов. Финальная production-сборка Next.js проверена локально; полный запуск финальной Compose-сборки не подтверждён |

Предыдущие проверки зависимостей API/frontend не выявили известных уязвимостей. После установки тяжёлого ML-окружения полный аудит транзитивных ML-зависимостей не повторялся; это не утверждение об отсутствии уязвимостей во всём образе. Backend-образ крупный (около 10 ГБ): CPU-исполнение не исключает CUDA-пакеты из зависимостей PyTorch.

## Что осталось для интеграции

1. Зарегистрировать реальный LTI Developer Key, установить приложение и войти двумя школьными учётными записями.
2. Проверить HTTPS/reverse proxy, cookies, исходящие соединения к Canvas/Letovo и права отдельного Canvas REST-токена на целевом сервере.
3. Проверить реальный курс: полноту импорта, допустимость публикации для всех учеников, минимум 10 контрольных вопросов, цитаты и порог сходства.
4. Измерить нагрузку, холодный запуск и индексацию. Настроить резервные копии, сроки хранения и восстановление.
5. Активировать CI: шаблон `deploy/github-actions-pilot.yml` подготовлен, но раньше GitHub отклонил установку workflow из-за отсутствующего разрешения у OAuth-токена.

Статус: **подготовлено для технической приёмки ограниченного пилота**. Массовая эксплуатация и полная совместимость со школьной установкой Canvas пока не подтверждены. Логичные следующие функции и отличия от исходных пожеланий перечислены в технической документации.
