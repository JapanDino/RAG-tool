# Queued specifications

Queued specs describe approved future vertical slices that cannot or should not
be active yet. They are not permission to implement several slices in parallel.

Before implementation, move exactly one queued spec to `active/`, update its
verified context, and keep every other queued spec unchanged.
